# Configurações do Servidor
NODE_ENV=development
PORT=3001
API_VERSION=v1

# Database PostgreSQL
DATABASE_URL="postgresql://username:password@localhost:5432/metalgalvano?schema=public"

# Firebase Admin SDK
# Opcional: se rodando no Google Cloud (Cloud Run / GKE), preferir usar ADC (Application Default Credentials)
# - Para *local* utilize as chaves abaixo (preencha com o JSON do serviço)
# - Em produção (Cloud Run), remova FIREBASE_PRIVATE_KEY e a aplicação usará a Service Account do Cloud Run
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY\n-----END PRIVATE KEY-----\n" # remover este campo se estiver usando ADC em Cloud Run
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# Firebase Storage
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Cloud SQL (se usar Cloud SQL com Cloud Run)
# Example: project:region:instance
CLOUD_SQL_INSTANCE_CONNECTION_NAME=project:region:instance

# Segurança
JWT_SECRET=your-super-secret-jwt-key-change-this
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logs
LOG_LEVEL=info
